use caso3;
DELETE FROM actores_x_contrato where actor_id>=0;

select * from actores_x_contrato;
INSERT INTO actores_x_contrato (actor_id, contrato_id, porcentaje ) values (1, 10, 0.2)
INSERT INTO actores_x_contrato (actor_id, contrato_id, porcentaje ) values (2, 10, 0.2)